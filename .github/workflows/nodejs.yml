name: deployment

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@master
    - uses: actions/setup-node@master
      with:
        node-version: 12.x
    - run: npm i
    - run: npx hexo g
    - run: export
  deploy:
    runs-on: ubuntu-latest
    steps:
      - run: export
      - run: echo ${{ secrets.firebase }}
    
    
    
